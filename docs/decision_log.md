# Decision Log

Per issue [27](.github/issues/27-decision-and-failure-logs.md). A decision that exists only in a chat message does not count.
Every row must name a **criterion** and a **measured number**, not a preference.

Research-type issues (04, 05, 06, 11) must each append at least one row here as part of their own acceptance criteria.

## Weekly five-question check-in

Log the answers at the top of this section each week, newest first.

### Week of ____

1. What did we complete?
2. What evidence do we have?
3. What failed?
4. What is blocking us?
5. What will we do next?

---

## Decisions

| Date | Decision | Options considered | Criterion used | Evidence / measurement | Decided by |
|---|---|---|---|---|---|
| 2026-09-16 | Added `vision_utils.identify_frame()`, which tries both `find_poster_region()`'s precise narrow projection and its own source barrier box through `identify()` and keeps whichever is accepted (or higher-confidence, if both), instead of committing to the narrow projection alone; wired it into `identify_at_station()` (live controller) and `tools/eval_vision.py` (report) | Keep the single narrow crop (verified `headphones` fails live: every frame 0.28-0.42 confidence, mission ends `FAILED`); find a purely-geometric rule for when to widen the crop (tried a darkness centroid and a brightness-notch column detector, both rejected -- see failure log for why neither separated the real failure from ordinary working frames); try both interpretations and let `identify()`'s own accept/reject logic decide, accepting a small, measured new-false-accept cost | Whether trying a second interpretation of the same detected barrier region, only when the classifier itself would accept it, improves real recall without regressing any frame that already worked, checked against the full 103-frame set rather than the live failure alone | Direct old-vs-new comparison on all 103 real frames: 0 regressions of previously-correct results, 12 frames flipped from wrong/`NO_MATCH` to correct, only 2 new false-accepts (`camera`, from a `soda_can` and a `backpack` frame) -- both exclusively at the synthetic 1.0 m test distance, well past the real ~0.295 m observe standoff, so not a distance the live robot actually stops at. Full-harness re-run: precision/recall improved on 6 of 8 labels (`headphones` recall 0.455 -> 0.636, `fire_extinguisher` 0.375 -> 0.625, `running_shoe` 0.714 -> 0.929, plus `soda_can`/`camera`/`wall_clock` gains); `camera`'s FP count rose 1 -> 3, all three at that same unrealistic distance. Confirmed live in Webots: `headphones` mission that previously ended `FAILED` now succeeds, all first 3 frames agreeing at 0.76-0.77 confidence, `FINAL STOP` 0.099 m from observe | Claude Code, after the user asked for a real fix (not a threshold nudge) and confirmed with a live camera-frame example (a stock toaster photo whose two dark slots visually explain the same failure mode) that the crop-centering theory was worth pursuing |
| 2026-09-16 | Removed `fire_extinguisher`'s per-label floor from `MIN_REFERENCE_SIMILARITY_BY_LABEL` (`identify()`, Issue #8/#9), leaving it on the shared default (-0.05) like every label except `coffee_mug`/`wall_clock` | Keep the 0.20 floor (protects the report's 0/20 distractor false-accept number, but guarantees `fire_extinguisher` never gets accepted live); lower it to a smaller positive value that still blocks `books_a.png`'s 0.167 (not possible -- see evidence); remove the per-label floor entirely | Whether any floor value exists that accepts real `fire_extinguisher` crops while still rejecting the known false-accept distractors, checked against measured scores rather than picked by feel; and whether removing it could ever cause a *different* real target's frame to be wrongly accepted as `fire_extinguisher` | A manually traced, pixel-perfect crop of the real in-game poster (no shadow/floor contamination) still only scored 0.115 on this metric despite 97.5% classifier confidence on the same crop -- lower than the `books_a.png`/`books_b.png` distractors it was meant to block (0.167/0.172), so no floor value separates them; the module's own confusion matrix (`docs/data/vision_confusion_matrix.csv`) showed zero false positives for `fire_extinguisher` against every real frame from any of the other 7 stations, so removing the floor cannot cause cross-target confusion. Re-ran `tools/eval_vision.py` after removing it: best-crop station accuracy 7/8 -> 8/8, distractor false-accepts 0/20 -> 1/20 (`books_a.png` only; `books_b.png` still correctly rejected on `MIN_CONFIDENCE`) | Claude Code, after the user weighed the trade-off (real live-mission detection vs. one distractor-rejection report number) and asked to proceed |
| 2026-09-16 | Rescoped Issue #20's test matrix from 3 worlds x 8 targets x 2 repeats (48 runs) down to 3 worlds x 6 representative targets (3 boundary, 3 interior) plus extra repeats only on a known-borderline target, ~20 runs total | Run the full 48-run matrix as originally written; rescope to what the brief actually requires; skip the matrix entirely | Whether the brief itself (not this issue's own prior wording) requires exhaustive per-cell repetition, checked directly against the source document rather than assumed | `notes_for_project/workshop8-p3-group-project.pdf`, Stage 8 and "Testing Strategy" sections: asks only for a "small test matrix" covering start/target/station-type/repeated-run as dimensions, with repeats specified only "when tuning parameters," not for every cell. No 48-run or per-cell-repeat requirement exists in the brief; that scope was this issue's own prior addition | Claude Code, after the user asked to cross-check the issue's scope against the actual project document rather than proceed on the issue file's wording alone |
| 2026-09-16 | Chose nearest-first (recomputed via `astar` cost after each visit) as the station visit-order strategy (Issue #15) | Nearest-first by computed A* path cost; a fixed geometric sweep (stations sorted by angle around the arena centre, same order regardless of start) | Total path cost (cells) to visit all 8 stations from each of the 3 starts, plus a real Webots measurement of simulated time for the worst case | `tools/check_visit_order.py`: nearest-first beat the fixed sweep from every start (A: 155 vs 162 cells; B: 138 vs 173; C: 141 vs 188 cells). Worst-case start (A) measured for real in Webots: 173.8 s simulated time, 66 s under the 240 s budget, visit order matching the computed prediction exactly | Claude Code, from computed A* costs and a real Webots run — see `docs/decision_visit_order.md` |
| 2026-09-16 | Chose selective inflation (radius 4 cells) as the obstacle-clearance policy for planning (Issue #11) | Raw grid (no inflation); uniform one-cell inflation; selective inflation at radii 1-5 | Policy must pass all 24 start/station reachability pairs (BFS, 4-connected) on the 40x40 occupancy grid | `tools/check_reachability.py`: raw grid passes all 24; uniform inflation loses S2, S4, S6, S8 (interior stations sealed off, corridors 1-2 cells wide); selective inflation only restores all 8 at radius >=4 (radius 1-3 still loses the same four) | Claude Code, from computed BFS reachability data — see `docs/decision_clearance.md` |
| 2026-09-16 | **Correction**: `find_poster_region()`'s IoU result was reviewed and correctly found not actually passing (12/20, 12/15 unclipped -- a strict reading of "boxes overlap with IoU >= 0.5" means the labelled set should pass, not that the mean clears 0.5), and the issue was reopened rather than left closed on a caveat | Accept the caveat and leave it closed; search additionally within overlapping left/right sub-windows (in addition to the full frame) so an unrelated dark object elsewhere in frame can't force a merge that no single full-zone threshold separates; keep clipped stress cases separate from the required IoU set | Whether every required unclipped labelled frame individually clears IoU >= 0.5, not just the mean | All 3 failing frames traced to the same root cause: an unrelated object (a wall decoration; another station's barrier) sits close enough to the real target that even the narrowest value band merges them across the whole zone -- confirmed by inspecting the raw frames and masks, not assumed. Sub-window search fixed it; then 5 more unclipped labels were added and the 5 clipped diagnostics were split into a separate stress file so the required labelled set is unambiguous: 20/20 (100%) required unclipped frames >=0.5, mean IoU 0.787. A side effect (one new negative-frame false positive) was found, and a stricter-solidity fix for it was tried and reverted after real data showed it cost a real unclipped detection and an IoU pass to fix a test with large existing headroom (>=10 required, 15-17/18 available either way) -- kept the version that's stronger on the primary, tightly-specified criteria | Claude Code, from real captured/scored/hand-labelled data, reviewed and directed by Nora — see `docs/find_poster_region.md` and `docs/find_poster_region_notes.md` |
| 2026-09-16 | `find_poster_region()` locates the poster via the barrier landmark + known geometric fraction, not by thresholding the poster panel's own brightness directly (Issue #7) | The issue's own suggested method (threshold the bright poster apart from the dark barrier); the barrier-landmark method actually implemented; a per-frame "distance from modal barrier colour" mask (also tried) | Whether the method holds up across all 8 targets, tested on real frames, not reasoned about | Direct poster-brightness thresholding fails on dark targets: the backpack/headphones poster measured barely brighter (~35) than the plain barrier body itself (~35) at Station S3 (Issue #6 data). The barrier-landmark method scored 100% detection on 36 real unclipped frames, mean IoU 0.662 (12/15 >=0.5) against hand-labelled ground truth, and correctly returned `None` on 17/18 real no-poster frames after two rounds of real bugs found via the hand-labelled IoU test and fixed (a degenerate "found nothing more specific than the whole search window" candidate winning by size; false positives on sky/floor/plain-barrier content) | Claude Code, from real captured/scored/hand-labelled data — see `docs/find_poster_region.md` |
| 2026-09-15 | Chose ResNet-18 (frozen backbone, retrained head) over ORB for target identification (Issue #6) | Option A: ORB keypoints + ratio-test matching; Option B: ImageNet-pretrained ResNet-18, frozen backbone, retrained `Linear(512,8)` head on augmented references | Higher top-1 accuracy on a real held-out test set of captured frames from all 8 stations, AND lower per-frame runtime against the 32 ms control timestep, AND accuracy concentrated in the distance range where identification actually happens (close/mid-range on approach) | 84 real frames captured across all 3 worlds/8 stations (`docs/data/vision_eval_captures/`); 8 excluded for an unreliable crop (see `docs/decision_vision_approach.md`). ResNet-18: 54/76 = 71.1% top-1, 9.1 ms/frame. ORB: 41/76 = 53.9%, 20.5 ms/frame. Not a uniform win: ORB scored better at the single longest distance tested (1.0 m, n=14) -- stated plainly in the record rather than smoothed over | Claude Code, from real captured/scored data — see `docs/decision_vision_approach.md` |
| 2026-09-14 | Set `WARN = 120`, `STOP = 300` for `ps0`-`ps7` (Issue #4) | Workshop's example value (~80); a higher pair tuned to the wall's late, sharp rise; the measured pair below | WARN must sit clearly above the ~65-95 sensor noise floor seen with no obstacle present; STOP must sit below the weakest real contact reading measured (barrier B1's resting contact, 355) so it reliably fires for every obstacle class, not just the strongest | Logged wall, barrier B1 and station S4 approaches to real distance (`docs/data/proximity_*.csv`, plotted in `docs/data/proximity_calibration.png`). All three stay flat (~65-95) until roughly 0.03-0.10 m from contact, then rise sharply. Max reading at contact: wall 1764, station S4 1769, barrier B1 (corner-on) only 355 | Claude Code, from logged Webots data — see `docs/proximity_calibration.md` |
| 2026-09-14 | Confirmed `STOP = 300` still holds after re-testing B1 at 3 approach angles (Issue #4 follow-up) | Re-tune `STOP` down to chase the higher transient spikes seen (up to 1538); or keep 300 | The wedged/settled reading (not the momentary spike on first contact) is the only reliably reproducible signal — `STOP` must sit below the lowest settled value seen across all attempts, not the highest spike | 3 more B1 approaches at x-offsets -0.08/0.0/+0.08 (`docs/data/proximity_barrier_B1_angles.csv`). All 4 B1 attempts now on record: transient peaks 355-1538 (no pattern by offset), settled/wedged readings consistently ~350-382 | Claude Code, from logged Webots data — see `docs/proximity_calibration.md` |
| 2026-09-14 | Revised `STOP` from 300 down to 150, after extending the angle sweep to all 3 obstacle classes (9 approaches total) apparently found a worse case than B1 | Keep 300 (only validated against B1); lower to 150 (validated against the apparent new worst case) | STOP must sit below the weakest near-contact reading found across *every* obstacle class tested, not just the one that happened to be checked first | Wall approach at +0.08 m offset reached only 207 right at contact and was still rising (`docs/data/proximity_wall_angles.csv`). **This evidence turned out to be invalid — see the next row.** | Claude Code, from logged Webots data — see `docs/proximity_calibration.md` |
| 2026-09-14 | **Correction**: the wall's 207 reading was a test bug, not a real sensor property — `STOP = 150` re-justified from corrected data instead | Revert to 300 (original, now also unverified against a full sweep); keep 150 but re-verify with a bug-free method | Same as above, but the offset target must be computed from a fixed reference (the true start / the wall's known coordinate), not recomputed from the robot's current position after each back-off, which drifts | Bug: offset targets recomputed from post-backoff position drifted up to 0.14 m off the true wall (confirmed from logged target coordinates); see `docs/failure_log.md`. Re-ran with `wall_offset_target()` (9 offsets) and `fixed_direction()` (station S4, 7 offsets): wall now spikes >1000 at every offset, no weak case; station S4's weakest verified case reached 226 at contact (crosses 150 with ~0.007 m margin); B1 unaffected by the bug, still ~350-380 | Claude Code, from logged Webots data — see `docs/proximity_calibration.md` |
