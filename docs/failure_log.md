# Failure Log

Per issue [27](.github/issues/27-decision-and-failure-logs.md). Every test run that fails, including during development,
gets a row the same day it happens — failures are engineering evidence for the report, not something to hide.

Target: at least six rows by the end of M4, spanning both vision and navigation. Issue [21](.github/issues/21-failure-cases-and-fixes.md)
turns this into the report's failure-cases chapter, so name *where the first incorrect decision occurred* once known.

| Date | What was tested | What failed | Suspected cause | What was changed | Result after change |
|---|---|---|---|---|---|
| 2026-09-17 | Issue #20's full 25-run test matrix (3 worlds x 8 targets, real Webots, GUI/fast mode), all runs recorded in `docs/data/results_matrix.csv` | Not a mission failure -- all 25 runs reached `SUCCESS`. But in every World B and World C run whose target was *not* `headphones` (6 of the 8 non-`headphones` runs in each of those two worlds, since `PLAN` visits S7 first from both start poses), `IDENTIFY` at S7 returned `NO_MATCH` at 0.31-0.37 confidence for the full 20-frame window, only resolving to the correct `headphones` label (0.67-0.71) on the automatic retry-from-observe-pose path. This cost each of those runs an extra ~20-40 s and a full navigate-back-and-forth cycle it didn't strictly need. Oddly, the one run that queried `headphones` directly as the *target* in World B (row 16) read it correctly on the very first pass at 0.88 confidence with no retry at all -- same station, same approach pose, same identify_pose, same code path, same fixed classifier seed, but a different first-pass result depending on what target was being searched for | Not yet root-caused. The simulation is otherwise fully deterministic (fixed `IDENTIFIER_SEED`, no injected sensor noise, confirmed by `soda_can`/World A being bit-for-bit identical across two full runs), so *something* differs between the "searching for headphones" and "searching for something else, currently looking at S7" cases even though the crop location and classifier weights should be identical either way -- possibly a small camera-frame or pose difference from a subtly different navigation history before S7, not yet inspected with a saved frame. World A never shows this pattern at all (S7 is visited last there, after the identifier is already warmed up and several other stations already read cleanly) | Nothing changed -- logged only, per Issue #20's own scope (root-causing is Issue #21's job, not #20's). Every affected run still reached `SUCCESS` via the existing retry path, so this did not block the matrix, but it is a real, reproducible confidence weak point at S7 worth investigating for the robustness discussion | Not investigated further this session. Next step for Issue #21: re-run one affected case (e.g. World B, target `soda_can`) with `DEBUG_CAPTURE_DIR` set to save the real S7 frames from both the first-pass and the retry, and compare them directly to the row-16 `headphones`-direct frame to see whether the crop itself differs or only the classifier's read of an identical crop differs |
| 2026-09-17 | All 6 worlds run headlessly after the readability/shortening pass (`worlds/training_start_{A,B,C}` + `test_worlds/test_start_{A,B,C}`), target `headphones`, full vision, telemetry off | 5 of 6 passed; `test_start_B` returned `FAILED` (all 8 stations visited). In that world's full-reversal mapping `headphones` is at S2. The robot navigated to S2 correctly and the classifier then alternated between the right and wrong label across consecutive frames: `headphones` 0.71, `camera` 0.75, `camera` 0.74, `headphones` 0.72, `camera` 0.72 -- `camera` reached 3-of-the-last-5 first and S2 was marked visited with a confident wrong label. S7 (`coffee_mug` here) also gave 20 frames of `NO_MATCH` at 0.00. First incorrect decision: `IDENTIFY`'s consensus vote at S2, not `PLAN`/`NAVIGATE` | `headphones`/`camera` are confusable at this station's approach geometry, and the 3-of-5 consensus rule protects against a single bad frame but not against a 3:2 split between two confusable classes -- a distinct failure mode from the two already catalogued in `docs/shuffled_worlds.md` (borderline confidence, and confidence never rising): here the system is confidently wrong rather than unsure | Nothing changed -- logged only. The run was a verification of a readability-only refactor, and the three training worlds reproduced their pre-refactor numbers exactly, so this is a pre-existing vision-accuracy gap rather than a regression. Note no prior recorded run of `headphones` in world B exists to compare against | **Fixed same day.** Root-caused with `DEBUG_CAPTURE_DIR`: the crop was correct and identical on all 5 frames `(42, 7, 53, 54)` and visibly shows the headphones, so this was classifier confusion, not a `find_poster_region` fault. Reference similarity separated the cases (-0.029 for the false `camera` vs +0.123/+0.176/+0.175 for the real `camera` at S4), so a per-label floor of 0.00 for `camera` was added to `MIN_REFERENCE_SIMILARITY_BY_LABEL` (`docs/decision_log.md`). S2 now reads `headphones` 0.71/0.72/0.72 and confirms on the first 3 frames; the world goes SUCCESS at S2, 0.099 m, in 65.28 s (was 168.58 s and FAILED). All 6 worlds pass, the other 5 unchanged to 3 d.p. |
| 2026-09-16 | First full real-vision (non-stub) mission run through the Issue #16 state machine, start A, target `soda_can` | `IDENTIFY` returned `NO_MATCH` on all 12 frames at S1 (0.00 confidence every time) -- the one station that should have matched -- then wrongly confirmed `headphones` at S7, ending in `FAILED` | `Navigator` (Issue #14) only ever targets an (x, y) position, never a heading -- `observe_yaw` from `CONFIG["stations"]` was never referenced anywhere in the controller. The robot reached S1's exact observe position but arrived facing whatever direction its last waypoint happened to leave it in, not necessarily at the poster, so the camera never saw it | Extended `Mission._observe()` to rotate to `station["observe_yaw"]` (with its own step-budget failure path) before starting the settle countdown, instead of treating "reached the position" as "ready to identify" | Fixed: re-run reached S1 with real 3-frame consensus (confidences 0.64, 0.70, 0.71) and stopped there correctly |
| 2026-09-16 | First waypoint-following smoke test (Issue #13), start A to S8 | `ValueError: cannot convert float NaN to integer` in `world_to_grid()`, from `plan_path_to()` called before the first `robot.step()` | GPS/IMU read back NaN until the simulation has taken at least one step after `enable()`, so `get_pose()` returned NaNs for the initial pose | Added one `robot.step(timestep)` at the top of the test harness before any pose read | Fixed: re-run reached S8's observe position within 0.053 m |
| 2026-09-16 | Second waypoint-following smoke test, checking the logged cross-track/heading-error data made sense | Recomputed heading error from the logged CSV showed implausibly large values (mean ~1.7 rad) even though the robot visibly reached the goal | The test harness logged `waypoints[0]` as the target on every row, never updating it as `follow_path` internally advanced to later waypoints -- a logging bug, not a control bug | Changed `follow_path` to `yield` the `(x, y)` waypoint it is currently driving to, and had the harness log that instead of the stale first waypoint | Fixed: recomputed heading error and cross-track deviation from the corrected logs matched the visibly correct trajectory (max cross-track 0.028-0.041 m across all `KP_HEADING` values tested) |
| 2026-09-16 | Issue #14 deliberate-obstacle-facing test: rotated to face the nearest obstacle from start A, then handed off to `Navigator` targeting S3 | `STUCK_RECOVERY` fired at step 62 (within the first `STUCK_WINDOW_STEPS`), even though nothing was blocking the robot -- no `STOP`/`AVOID` had fired at all | The first planned waypoint was roughly 180 degrees from the post-rotation heading. Issue #13's own speed-reduction rule (cut forward speed when heading error is large) combined with `MAX_SPEED` clamping meant the differential term saturated to a near-pure in-place rotation while turning in -- genuinely very little net XY displacement during that phase, which is correct, expected behaviour, not a stuck robot | Widened `STUCK_WINDOW_STEPS` from 60 to 200 (~6.4 s), comfortably longer than the worst-case turn-in time (~pi radians at the achieved ~0.8-1 rad/s body rate), so a normal reorientation-then-drive sequence shows real net progress by the end of the window while a genuinely wedged robot still does not | Fixed: re-run with the wider window showed no false trigger |
| 2026-09-16 | Re-run of the deliberate-obstacle-facing test, this time actually approaching to within `WARN` range before handing off to `Navigator` (the first version stopped too far away for `ps` sensors to react at all) | `Navigator` entered `SEARCH` ("no path found") and never recovered -- looped `STUCK_RECOVERY -> RECOVER_BACKOFF -> RECOVER_ROTATE -> SEARCH` for the rest of the run, and 12 consecutive above-`STOP` readings were logged (acceptance criterion 2 allows at most 3) | First suspected: the robot's cell fell inside the Issue #11 inflation margin (not a real obstacle). Patched `plan_path_to()` to fall back to the raw grid when only the margin was the problem -- still failed. Checked the actual cell directly: the robot's real, collision-free pose (`-1.2457, -0.1515`) rounds to grid cell `(21, 7)`, which the *raw* grid also marks as an obstacle (`grid[21,7] == 1`). The 0.1 m grid is quantizing a near-wall position that is physically clear of the wall mesh onto the obstacle side of a cell boundary | Simplified the fix: since the robot is physically standing at its current cell right now (Webots collision already guarantees that), that one cell is patched free for the planning call regardless of what either grid says, rather than trying to distinguish margin-blocked from raw-blocked | Fixed: re-run reached S3 within 0.058 m, with `AVOID -> STOP -> STOP_ROTATE -> REPLAN -> FOLLOW` all logged correctly |
| 2026-09-16 | Re-run of `tools/eval_vision.py` after trying to quiet Matplotlib font-cache warnings | The script tried to download ResNet-18 weights again and failed offline because `XDG_CACHE_HOME` had been moved to `/tmp`, changing Torch's cache path | The font-cache cleanup environment variable was set too early, before torchvision loaded the already-cached weights | Moved the `XDG_CACHE_HOME` override into the plotting functions, after the identifier has loaded, leaving Torch's default cache alone | Fixed: rerun completed with 7/8 best target crops and 0/20 distractor false-accepts |
| 2026-09-16 | Issue #9 distractor-rejection probe on all 20 `textures/distractors/*.png` images at the shipped Issue #8 thresholds | 4/20 distractors were accepted as target labels (`books_a`, `keyboard_b`, `soccer_ball_a`, `soccer_ball_b`) | The 8-way classifier always chooses the nearest target class for unrelated images; confidence alone is not an open-set detector | Added a same-reference sanity check in `identify()` after the ResNet prediction, rejecting confident matches that do not resemble the predicted target's reference image enough | Fixed: `tools/eval_vision.py` reports 0/20 distractor false-accepts while best-station target accuracy stays 7/8 |
| 2026-09-14 | Proximity-calibration test (Issue #4) driving at wheel speed 8.0 | Webots console flooded with `RotationalMotor ... requested velocity 8 exceeds maxVelocity = 6.28` every step | The starter code's `MAX_SPEED = 10` clamp was higher than the e-puck's real wheel motor limit (6.28 rad/s in Webots) | Set `MAX_SPEED = 6.28` in `group_project_controller.py`; lowered the calibration test's `APPROACH_SPEED` to 6.0 | Fixed - re-run reached the target area, revealed the next failure below |
| 2026-09-14 | Wall approach in the proximity-calibration test, from world C's start (1.46, 0.90) toward a hardcoded wall point (-2.0, 0.0) | Robot collided with the B2 navigation barrier partway there and got wedged, so it stayed in contact for the rest of the run (the next two tests ended in 9 and 0 steps) | The straight line from that particular start to the fixed wall target happened to clip B2's footprint; no obstacle avoidance exists yet (that's Issue #14) | Replaced the fixed wall target with `nearest_wall_point()`, which aims at whichever wall is closest to the robot's actual start pose - a straight shot along one axis instead of a diagonal across the arena | Fixed the wall test, but chaining straight into the next test (below) broke differently |
| 2026-09-14 | Chained barrier_B1 approach, starting right after the wall test finished (post-wall position, not the world's real start) | Robot drove into the S4 station barrier on the way to B1 (0.79 m short of B1, high reading) - S4 happened to sit almost exactly on that path | Chaining three approaches back-to-back means each leg starts from an unpredictable position left by the previous one, not a known collision-free start; a 25-step back-off wasn't enough to clear the geometry | Redesigned so each world (A/B/C) runs exactly one calibration test from its own known start pose (detected via `nearest_start_id()`), instead of chaining three tests in one run | Fixed for the 3-point sweep, but a related drift bug (below) showed up in the 9-point fine sweep |
| 2026-09-14 | Fine-grained wall angle sweep (offsets -0.15 to +0.15 m), 9 chained attempts with back-off between each | Readings for offsets >=0.09 m came back at the noise floor (73-107), which looked like a total sensor blind spot | The offset target was computed relative to the robot's *current* (post-backoff) position, not the original start - small heading error during each approach meant each back-off drifted slightly, and by later attempts the "target" had drifted 0.05-0.14 m off the actual wall line into open space. The robot was stopping short of the wall entirely, not failing to detect it | Replaced with `wall_offset_target()`, which offsets along the wall's known fixed coordinate directly, independent of the robot's current position | Not yet re-verified — pending next run |
| 2026-09-16 | Issue #19 shuffled world A (`test_start_A.wbt`, `camera` moved from its training station S5 to S6), full real mission run, target `camera` | Robot correctly reached S6 and the identifier repeatedly recognised `camera` there (frames 1, 2, 6 scored 0.76/0.75/0.69 and label `camera`), but never got 3 *consecutive* agreeing frames -- single disagreeing `NO_MATCH` frames kept breaking the streak (2/3 then reset) -- so S6 was marked visited with no consensus and the mission ended in `FAILED` after all 8 stations | Vision layer is working correctly (it is reading the shuffled poster's content, not a memorised station), but per-frame confidence hovers close to `MIN_CONFIDENCE` at this specific approach angle, so ordinary frame-to-frame noise is enough to break `IDENTIFY_CONSENSUS_FRAMES`'s consecutive-frame requirement | None yet -- flagged for issue 21 rather than patched under issue 19, which is scoped to building/evaluating the shuffled worlds, not fixing consensus logic | Not fixed. Candidate improvement for issue 21: relax to "N-of-M agreeing frames" instead of strictly consecutive, or increase settle time at borderline-confidence stations |
| 2026-09-16 | Issue #19 shuffled world B (`test_start_B.wbt`, `backpack` moved from its training station S3 to S6), full real mission run, target `backpack` | All 12 frames at S6 returned `NO_MATCH` with confidence stuck at 0.24-0.35, well under `MIN_CONFIDENCE` -- mission ended in `FAILED` after all 8 stations | Unlike the camera case above, the model never seriously considered `backpack` at this station/angle at all (no `label=backpack` on any frame) -- looks like a genuine per-class/per-viewpoint accuracy gap at S6 (an interior station), consistent with the ~71% top-1 accuracy already measured in issue #6, not something the shuffle introduced | None -- same reasoning as the row above | Not fixed. Needs a targeted capture-and-retrain or standoff-distance check for `backpack` at interior stations (issue 21/22 material) |
| 2026-09-16 | Issue #19 shuffled world C (`test_start_C.wbt`, `wall_clock` moved from its training station S8 to S7), full real mission run, target `wall_clock` | All 12 frames at S7 returned `NO_MATCH` at exactly 0.00 confidence -- not just low, but the poster crop step itself appears to have found nothing usable | Suspect `find_poster_region()` failed to isolate the poster at this station/world's specific approach geometry (world C's start pose differs from A/B, so the final approach heading into S7 differs too) rather than a classifier problem -- needs a saved frame to confirm, not yet done | None -- flagged for issue 21 | Not fixed. Needs a saved camera frame from this exact run to tell a crop failure from a lighting/geometry issue before proposing a fix |
| 2026-09-16 | Issue #19 shuffled world A, full real mission run, target `wall_clock` (now at S1, a boundary station used heavily in prior calibration/testing) | All 12 frames at S1 returned `NO_MATCH`, confidence 0.24-0.36, never even briefly matching the `wall_clock` label | Even at the best-characterised station in the whole project, `wall_clock` itself does not classify confidently -- points to `wall_clock` being a weak class for the trained ResNet-18 head generally, independent of station or shuffling | None -- flagged for issue 21 | Not fixed. `wall_clock` should be added to the next accuracy re-evaluation pass as a suspected weak class |
| 2026-09-16 | Root-cause pass on the three weak-target rows above (`wall_clock`, `fire_extinguisher`, `backpack`), following up on a Codex review comment. No new Webots run: re-examined the existing saved frames in `docs/data/vision_eval_captures/` and `docs/data/vision_target_predictions.csv`, plus ran `vision_utils.find_poster_region()` and `_barrier_candidates()` directly (cv2-only, no torch needed) against the S4 (`fire_extinguisher`) frames that scored `no_crop` | Three distinct causes, not one: (1) `fire_extinguisher` -- 5/8 evaluated frames are genuine crop failures (`find_poster_region()` returns `None`), and pixel-level inspection of `B_S4_d0p420_hp00.png` shows why: the extinguisher's black hose (HSV value ~34-46) is far darker than the barrier's actual grey panel (~88-89), so no single `_BARRIER_VALUE_BANDS` band isolates the barrier alone -- a narrow band grabs only the thin hose (rejected on `_BARRIER_ASPECT_MIN`, aspect 0.25-0.35) and a wide band merges the barrier with the floor (rejected on `_MIN_SOLIDITY`, 0.33-0.45). In the 3/8 frames that do crop, the classifier never once predicts `fire_extinguisher` as top class (capped ~0.26 confidence) -- a real crop bug plus a real classifier gap, both open. (2) `wall_clock` -- crop always succeeds and the raw prediction is already correct at every distance; it is a pure confidence problem, and specifically a close-range one: confidence is 0.20-0.61 (often below `MIN_CONFIDENCE=0.50`) under 0.45 m, but 0.92-0.99 at >=0.8 m in every captured frame. (3) `backpack` -- mixed: `no_crop` at close range in world C specifically, and even with a good crop confidence tops out at 0.59-0.62, never a clean match | None -- diagnosis only. No fix attempted: this environment has `torch` but not `torchvision` (and no Webots), so `tools/eval_vision.py` can't run here to validate a cropping or threshold change against the full 103-frame set the way `vision_utils.py`'s own header requires before trusting a change | Not fixed. Next session with `torchvision`/Webots available should: (a) for `fire_extinguisher`, try detecting the barrier by its known grey value band directly instead of "darkest region in frame", since that assumption is what breaks here; (b) for `wall_clock`, check whether OBSERVE's stopping distance can be pushed toward 0.8 m for this station, or add a `wall_clock`-specific lower confidence floor (precedent: `MIN_REFERENCE_SIMILARITY_BY_LABEL`) and re-run the distractor sweep to make sure it doesn't raise false-accepts; (c) for `backpack`, same close-range crop question as `fire_extinguisher` plus a possible retrain with more augmented close-up references. All three need `eval_vision.py`'s full run to confirm before shipping |
| 2026-09-16 | Live mission runs after the backed-off-viewpoint identify change still reported `NO_MATCH` at S4 (`fire_extinguisher`) and S7 (`headphones`). Installed `torchvision` in this session (previously missing, per the row above) so `tools/eval_vision.py` could finally run here; ran it plus a standalone script against every saved `vision_eval_captures/*_S4_*`/`*_S7_*` frame, printing `identify.last_result` for each | Confirmed the earlier hypothesis (a) for `fire_extinguisher`: implementing "detect the barrier by its own known grey value band" (a second, percentile-based anchor in `_barrier_candidates`, used only where the true-min anchor finds nothing) turned 3 of the 5 `no_crop` frames into correct, high-confidence `fire_extinguisher` crops (up to 0.867/0.825 margin) -- but all 3 are still rejected downstream by `below_reference_similarity` (0.113, -0.001, 0.144, all under the 0.20 floor), because the wider value band that finds the barrier panel also pulls in the shadow/floor strip directly beneath it, diluting the pixel-level comparison to the reference photo. `headphones` turned out to be a *different*, separate problem: crop already succeeds in most frames and the raw label is already correct; the two still-failing frames (`A_S7_d0p295_hm10.png` 0.435, `A_S7_d0p450_hp00.png` 0.360) sit just under `MIN_CONFIDENCE=0.50` with no crop or classifier-label error, and the nearest real distractor (`toaster_a.png`, 0.439) sits *between* them and the threshold -- lowering `MIN_CONFIDENCE` far enough to accept both real misses would also accept that distractor | Shipped the barrier second-anchor fix (`_ROBUST_VMIN_PERCENTILE`/`_ROBUST_VMIN_GAP_MIN` in `vision_utils.py`), gated so it only ever runs as a last resort when the true-min anchor finds nothing anywhere in the frame -- an earlier, ungated version of the same idea was caught regressing a real `running_shoe` frame (S6) in the full 103-frame re-run before shipping, because pooling both anchors let a small, unrelated, near-square blob win the aspect-ratio tie-break over the correct, larger candidate. No change attempted for the remaining fire_extinguisher reference-similarity gap or the two borderline `headphones` frames -- both need a real fix, not a threshold nudge, without more capture data | Partially fixed: `fire_extinguisher` crop failures dropped from 5/8 to 2/8 with zero regressions confirmed by `tools/eval_vision.py` (other labels' precision/recall and 0/20 distractor false-accepts unchanged) -- but station-level accuracy is still 7/8, since the newly-successful crops are blocked by reference-similarity, not classification. `headphones` unchanged (5 TP / 6 FN). Next: either tighten the poster-height projection so it stops including the shadow/floor strip for `fire_extinguisher`-shaped barriers, or add a `fire_extinguisher`-specific crop post-process before re-tuning `MIN_REFERENCE_SIMILARITY_BY_LABEL`; for `headphones`, better crops at the two borderline distances/headings are needed before the confidence floor can move |
| 2026-09-16 | Direct test of whether the reference-similarity metric itself, not crop framing, was the real blocker: manually traced the true poster boundary in `B_S4_d0p295_hp00.png` by inspecting raw pixel values (rows 0-54 = poster, confirmed bright/printed content; rows 55+ = grey strip/floor) and cropped exactly that region by hand, bypassing `find_poster_region()` entirely | The manual, pixel-perfect crop still only scored 0.115 reference-similarity -- essentially unchanged from the automatic crops (0.070-0.144) -- despite the classifier itself being 97.5% confident on that same crop. Also checked `docs/data/vision_confusion_matrix.csv`: `fire_extinguisher` has zero false positives against every real frame from any of the other 7 stations; the only things that ever score into this class are the two book-photo distractors, which do not and cannot appear inside the Webots simulation | Removed `fire_extinguisher`'s entry from `MIN_REFERENCE_SIMILARITY_BY_LABEL` (falls back to the shared -0.05 default). User was walked through the exact trade-off (real live detection vs. the report's distractor-rejection number) before agreeing to proceed -- see `docs/decision_log.md` for the full reasoning | Fixed for the cases where a crop is found: `fire_extinguisher` best-crop station accuracy went 7/8 -> 8/8 (TP 0 -> 3 of 8 evaluated frames), with the newly-accepted frames scoring 0.62-0.93 confidence. Cost: distractor false-accepts 0/20 -> 1/20 (`books_a.png` only; `books_b.png` still correctly rejected on `MIN_CONFIDENCE`), a known and now-documented exception rather than a hidden one. Not fully fixed: 5 of 8 evaluated `fire_extinguisher` frames still return `no_crop` or a low-confidence wrong label at certain distances/headings -- that's the earlier crop-detection limitation, unrelated to this threshold, and still open |
| 2026-09-16 | Follow-up attempt at the reference-similarity gap from the row above: pixel-traced `B_S4_d0p295_hp00.png`'s column values and found the robust-anchor box's extra ~30 px of height is a flat, uniform grey band (HSV value 88, same as the barrier panel itself) sitting between the poster and the floor, bridged in by the wide value band the robust anchor needs. Capped robust-anchor boxes at `bh <= bw` (the real barrel proto, 0.50 x 0.28 m, is never taller than wide) and re-ran `tools/eval_vision.py` | The height cap did trim the crop and raised classifier confidence on the 3 previously-fixed frames (e.g. 0.867 -> 0.931, margin 0.917), but reference-similarity on the clearest case *dropped* (0.113 -> 0.070) rather than improving -- checked directly against the one real distractor this threshold was calibrated on (`books_a.png`, refsim 0.167, `docs/data/vision_distractor_rejection.csv`): our genuine crop now scores *lower* than that unrelated books photo, so no threshold low enough to accept our crop could ever exclude that distractor. The floor-contamination theory was directionally right (it is fixable/measurable pixel content) but the reference-similarity metric itself doesn't reward the fix the way `identify()`'s own softmax confidence does | Kept the height cap (`cap_height` in `_barrier_candidates`'s `_boxes_for`) since it's a real, zero-regression improvement in crop tightness and classifier confidence -- confirmed via the full 103-frame + 20-distractor re-run (all other labels' precision/recall and 0/20 distractor false-accepts unchanged). Did not touch `MIN_REFERENCE_SIMILARITY_BY_LABEL["fire_extinguisher"]`: the books_a.png comparison above shows directly that lowering it far enough to accept the real crop would also accept that distractor, so this is not a safe threshold nudge | Not fixed. `fire_extinguisher` station-level acceptance is still blocked by `below_reference_similarity` even on a 0.93-confidence, 0.92-margin real crop. The `_reference_similarity()` metric (grey/Lab/hue-histogram match against the raw reference photo) itself needs revisiting for this label -- possibly weighting it down relative to classifier confidence for labels where it demonstrably fights the classifier instead of agreeing with it -- before any threshold change is safe |
| 2026-09-16 | Live Webots mission runs (world A, `training_start_A.wbt`, headless, two separate instances to avoid clashing with an interactively-open one), one targeting `fire_extinguisher` and one targeting `headphones`, after removing `fire_extinguisher`'s reference-similarity floor (see decision log). Added a temporary `DEBUG_CAPTURE_DIR` env-var hook to `identify_at_station()` (`group_project_controller.py`) to save every real camera frame during `IDENTIFY`, kept permanently (same idiom as the Issue #29 `STUB_PERCEPTION` hook) since it's zero-cost when unset | `fire_extinguisher` run succeeded end-to-end (confirmed `fire_extinguisher` at 0.95-0.96 confidence, `FINAL STOP` 0.089 m from observe). `headphones` run reached `state=FAILED` -- every frame at S7, both the first attempt and the automatic retry, scored confidence 0.28-0.42, always under `MIN_CONFIDENCE=0.50`. Inspected the saved real frames directly: the crop was landing exactly in the low-information gap between the headphones' two earcups (the poster's own bimodal two-lobe shape with a bright gap in the middle), not on either earcup's distinctive padded-ring face -- on the worst frame the crop was so far off it returned the *wrong* raw label (`running_shoe`) at 0.376, not just low confidence | Tried two purely-geometric detection rules to decide when to widen the crop (a darkness centroid, then a brightness "notch" detector on the crop's column profile) -- both rejected: the centroid landed back on the same midpoint for a symmetric two-lobe object (no shift possible by definition), and the notch detector scored an already-`accepted`, 0.91-confidence real `headphones` frame identically to the live failures, so it couldn't separate "bad crop" from "genuinely headphones-shaped." Added `vision_utils.identify_frame()` instead: try both the precise narrow projection and its own source barrier box (already computed, just previously discarded) through `identify()`, and keep whichever is accepted (highest confidence if both are). Verified against all 103 real frames before shipping: 12 improvements, 0 regressions of previously-correct results, 2 new false-accepts (`camera`, on two `soda_can`/`backpack` frames) -- checked those are both at the synthetic 1.0 m test distance, well past the real ~0.295 m observe standoff, so not a realistic live risk. Updated `tools/eval_vision.py`'s `load_target_rows()` to use the same function so the report stays honest | Fixed and confirmed live: re-ran the `headphones` mission after the fix -- all first 3 frames agreed at 0.76-0.77 confidence (fastest possible consensus), `FINAL STOP` 0.099 m from observe. Full-harness precision/recall improved on 6 of 8 labels (`headphones` recall 0.455 -> 0.636, `fire_extinguisher` 0.375 -> 0.625, `running_shoe` 0.714 -> 0.929, plus gains on `soda_can`, `camera`, `wall_clock`); `camera`'s false-positive count rose from 1 to 3, all three at the unrealistic 1.0 m distance -- see decision log for the full trade-off reasoning |
| 2026-09-16 | While re-validating the two fixes above against `docs/find_poster_region.md`'s hand-labelled IoU ground truth (not run earlier in this session -- a gap in the validation, not something the fixes broke): `find_poster_region()` picks the wrong box for `C_S1_d1p000_hp10.png` (`soda_can`, long range + 10 deg off-axis), landing on empty sky/background. Confirmed with the pre-session code that this exact bug already existed before any of today's changes -- the hand-labelled set had simply grown (36 -> 46 frames) since the report was last regenerated, so it was never re-checked. Extended `identify_frame()` from 2 boxes (top candidate + its barrier) to the top `_IDENTIFY_MAX_RANKED_CANDIDATES` (3) ranked candidates, hoping the same "try more, keep whichever the classifier accepts" fix would recover the correct (second-ranked) candidate here too | Confirmed via full-frame comparison that the top-3 extension is a real net improvement (8 more frames fixed, 0 regressions, one new `backpack`->`camera` false-accept at the same unrealistic 1.0 m distance) -- but the *exact* frame asked about is still wrong: the wrong candidate's own classification isn't merely low-confidence, it's a *confident, accepted* wrong answer (`camera` at 0.577, correct crop scores 0.479 -- just under `MIN_CONFIDENCE`), so trying more candidates can't help once the wrong one already wins outright. Tried four more signals looking for a way to reject the wrong candidate before or during classification: HSV value range (both wrong and right regions have wide range, for different reasons), Laplacian fine-detail variance (wrong crop scores *higher*, 376 vs 224 -- a sharp horizon line reads as more "detailed" than a printed graphic), and reference-image similarity (wrong crop scores 0.287 against the `camera` reference -- higher than several genuine `camera` frames, as low as -0.06) -- all three rejected, none separate the artifact from real content | Not fixed at the exact-frame level (5 total signals tried and rejected across this and the `headphones` case). Kept the top-3 extension since it's a clear net win. Verified the real-world risk directly instead of leaving it as a guess: ran two live Webots missions (`headphones` in world A, `soda_can` in world C -- the world the bad frame came from). Both succeeded cleanly; critically, `S3` (`backpack`) in the `soda_can` run correctly said `backpack` at 0.52-0.53 confidence, not the wrong `camera` guess seen offline. The real first-look distance (~0.8 m, straight-on, from the 0.5 m identify backoff) does not reproduce the failure condition (1.0 m *and* 10 deg off-axis) -- confirmed live, not just reasoned about |
| 2026-09-17 | Live Webots mission run, World C, target `coffee_mug`. User reported the robot reaching `S2` correctly, trying both the backed-off and final observe viewpoints, and returning `NO_MATCH` on all 40 frames despite the observe-pose confidence looking high (0.85-0.90) -- correctly guessed this pointed at an acceptance-gate rejection, not navigation/crop/classifier, and proposed the same debug-capture method already used for `fire_extinguisher`/`headphones`. Used the existing `DEBUG_CAPTURE_DIR` hook to save all 40 real `S2` frames and ran them through `identify_frame()` directly | Confirmed exactly as suspected: the 5 backed-off-pose frames sampled scored `raw_label=backpack` (crop/candidate-selection noise, a smaller separate issue) at 0.58-0.70 confidence, but the 5 retry-pose frames all correctly scored `raw_label=coffee_mug` at 0.86-0.90 confidence with an 0.82+ margin -- rejected every time on `below_reference_similarity` (0.014-0.018, against `coffee_mug`'s own 0.10 floor in `MIN_REFERENCE_SIMILARITY_BY_LABEL`) | Same fix as `fire_extinguisher`: checked the confusion matrix (zero cross-target false positives for `coffee_mug` from any other station) and the two distractors the 0.10 floor was protecting against (`keyboard_b.png` 0.092, `soccer_ball_a.png` 0.058) -- both score higher than the real crop, so no floor keeps both. Removed `coffee_mug`'s entry from `MIN_REFERENCE_SIMILARITY_BY_LABEL` | Fixed and confirmed live: re-ran the exact same mission -- `coffee_mug` confirmed on the first 3 frames (0.50-0.54 confidence), `FINAL STOP` 0.098 m from observe. Full-harness re-run: `coffee_mug` recall 0.500 -> 0.929 (7 -> 13 correct); distractor false-accepts 1/20 -> 3/20 (exactly the two predicted distractors, no other label regressed -- `running_shoe` incidentally improved, 1 FP -> 0) |
