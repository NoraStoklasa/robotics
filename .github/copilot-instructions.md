# Copilot Instructions

Read `AGENTS.md` at the repo root first. It has the repo-wide rules for every AI assistant working in
this project, including the required change-log entry in `docs/change_log.md` after any code, docs or
config edit.
